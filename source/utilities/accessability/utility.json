{
	"$schema": "../../utility-schema.json",
	"apiVersion": 1,
	"name": "Accessability",
	"slug": "accessibility",
	"description": "The Accessability utility provides a set of utility classes for improving the accessibility of elements in the interface.",
	"state": "stable",
	"entries": {
		"accessability": {
			"title": "Screen Reader",
			"content": "Using screen reader class is possible on all elements. When used on focusable elements, the element will become visible when focused, allowing keyboard users to interact with it.",
			"format": ".u-sr__only--{focus}",
			"description": {
				"focus": "Removes an elements visuals but is still accessible for screen readers. When using --focus modifier, the element will become visible when focused, allowing keyboard users to interact with it."
			},
			"modifiers": {
				"focus": "focusable"
			},
			"responsive": false,
			"examples": [
				{
					"title": "Simple Screen Reader Only Utility",
					"description": "Applying the accessibility utility class makes the element non-visible on the display but accessible for the screen reader.",
					"view": "accessability-main"
				}
			]
		}
	},
	"icon": "accessibility_new"
}
