export enum TooltipSetup {
    TriggerSelector = '[data-tooltip]',
    TooltipId = 'styleguide-tooltip',
}

export enum TooltipDom {
    AriaDescribedBy = 'aria-describedby',
}

export enum TooltipOffset {
    Top = 7,
    Viewport = 12,
    Arrow = 16,
}

export enum TooltipAttribute {
    AriaHidden = 'aria-hidden',
    Role = 'role',
}

export enum TooltipClassName {
    Root = 'o-tooltip',
    Visible = 'is-visible',
}

export enum TooltipStyleProperty {
    X = '--o-tooltip-x',
    Y = '--o-tooltip-y',
    ArrowLeft = '--o-tooltip-arrow-left',
}